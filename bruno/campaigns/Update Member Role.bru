meta {
  name: Update Member Role
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/campaigns/{{campaignId}}/members/{{targetUserId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "role": "gm"
  }
}
