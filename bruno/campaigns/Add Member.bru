meta {
  name: Add Member
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/campaigns/{{campaignId}}/members
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "userId": "{{targetUserId}}",
    "role": "player"
  }
}
